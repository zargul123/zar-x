GATE 3.2b-R4: the recorder's printed REPORT is now measured against the disk - the one line the Commander was ordered to judge it by

REPAIRS B11 (graded SERIOUS - three of three Step 2 answers bad). New check (l)
and sabotage B11, both inside __main__:

- the gate counts the rows ITSELF, before and after, and compares those numbers
  to the counts parsed out of the recorder's own printed line
- it runs the recorder TWICE. B11's claim and the truth are identical on run one
  and differ by 180 on run two, which is what every month looks like from now on
- the 4h boundary handled by construction: the bar is 'reported equals what
  actually landed on disk', never 'run two must report zero'
- a report line that fails to parse is a FAILURE, never a skip

GATE 3.2b-R4 PASSED, exit 0, zero failure marks, ELEVEN of eleven sabotages
caught. Production half sha256 9189c08f... identical before and after; every
diff hunk at or after line 263 with __main__ at 243. The original attack, re-run
as a REAL TEXT EDIT against the repaired file, now exits 1 with the named
diagnostic: 'run 2 BTCUSDT: the report claims 180 row(s) appended - the gate
counted 0 arriving on disk'.

S16 and F15 NOT REPAIRED - graded BORDERLINE, filed as R-016, the Commander
rules. Until he does, one line in either instrument can put a trade instruction
on the Brief with every gate green.

R-015 FAILED. R-017 filed against this repair by its own author, with three
doubts including the one it does not close: check (l) guards the counts and not
the window timestamps the same line prints.

Vault INTACT 6/6, lab byte-identical, Brief 3/3, data/oi_history unchanged.
